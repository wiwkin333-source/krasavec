# robots.txt for ГРАВИКОТ
# https://gravikot.ru
#
# SEO notes:
#  - /_next/static/* (CSS, JS, fonts) MUST be allowed so Googlebot and
#    YandexBot can render the page and understand its layout.
#  - /api/* is disallowed — internal endpoints, not for indexing.
#  - No `Host:` directive — Yandex now uses 301 redirects for mirror
#    consolidation. www → non-www is handled by a 301 in next.config.ts.
#  - Sitemap is served at /sitemap-raw (API route that bypasses ISR cache)
#    and stays in sync with the catalog data (no stale/404 URLs).
#    The standard /sitemap.xml route is cached by ISR on some hosts;
#    /sitemap-raw is guaranteed fresh on every request.

User-agent: Googlebot
Allow: /
Allow: /_next/static/
Disallow: /api

User-agent: Bingbot
Allow: /
Allow: /_next/static/
Disallow: /api

User-agent: YandexBot
Allow: /
Allow: /_next/static/
Disallow: /api

User-agent: Twitterbot
Allow: /
Allow: /_next/static/

User-agent: facebookexternalhit
Allow: /
Allow: /_next/static/

User-agent: *
Allow: /
Allow: /_next/static/
Disallow: /api

# Sitemap (guaranteed fresh — bypasses ISR cache)
Sitemap: https://gravikot.ru/sitemap-raw
